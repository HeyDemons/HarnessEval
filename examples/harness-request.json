{
  "schema_version": 1,
  "task": {
    "id": "arithmetic-smoke",
    "prompt": "Use the tools to retrieve alpha and beta, multiply the complete values, and return only the product."
  },
  "tools": [
    {
      "name": "lookup",
      "description": "Retrieve the number stored under alpha or beta.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "enum": ["alpha", "beta"]
          }
        },
        "required": ["key"]
      },
      "command": ["python", "/tools/arithmetic.py", "lookup"],
      "parallel": true,
      "read_only": true
    },
    {
      "name": "multiply",
      "description": "Multiply two numbers.",
      "parameters": {
        "type": "object",
        "properties": {
          "a": {"type": "number"},
          "b": {"type": "number"}
        },
        "required": ["a", "b"]
      },
      "command": ["python", "/tools/arithmetic.py", "multiply"],
      "parallel": true,
      "read_only": true
    }
  ],
  "policy": {
    "max_turns": 8,
    "protocol_repairs": 1,
    "dmas_agent_count": 10,
    "dmas_seed": 0,
    "dmas_forward_path_max_length": 3,
    "dmas_max_execution_times": 30,
    "lats_iterations": 2,
    "lats_generate_samples": 1,
    "lats_value_samples": 1,
    "lats_rollout_width": 1,
    "lats_tree_depth": 7,
    "lats_rollout_depth": 4,
    "lats_temperature": 1.0
  }
}
